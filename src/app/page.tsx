import AppShell from '@/components/layout/AppShell';

/**
 * Dashboard root page — Server Component wrapper.
 * All interactivity is inside AppShell (Client Component).
 */
export default function DashboardPage() {
  return <AppShell />;
}
