import AppShell from '@/components/layout/AppShell';

/**
 * /dashboard — Protected page.
 * Renders the full application shell (sidebar + header + content).
 */
export default function DashboardPage() {
  return <AppShell />;
}
